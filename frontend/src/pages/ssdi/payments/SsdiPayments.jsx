import "../../../styles/users.css";

export default function SsdiPayments() {
  return (
    <div className="page">
      <h2>Payments</h2>
      <p>
        Payments screen ready. Connect this page when `/api/payments`
        list endpoint is available on backend.
      </p>
    </div>
  );
}